spring:
  application:
    name: greeting
  cloud:
    config:
      enabled: false
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
greeting-service:
  greeting: "Holla"
  default-value: "Mundo"